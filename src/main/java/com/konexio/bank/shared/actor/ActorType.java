package com.konexio.bank.shared.actor;

/** Mirrors the {@code common.actor_type} domain: who a change is attributed to. */
public enum ActorType {
    CUSTOMER,
    STAFF,
    SYSTEM,
    PROVIDER
}
